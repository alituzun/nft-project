/* ==================== RESET & BASE ==================== */
*, *::before, *::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --bg-primary: #0d0d0d;
    --bg-secondary: #1a1a1a;
    --bg-card: #141414;
    --text-primary: #ffffff;
    --text-secondary: #b0b0b0;
    --pop-yellow: #FFE500;
    --pop-magenta: #FF00FF;
    --pop-cyan: #00E5FF;
    --pop-red: #FF003C;
    --pop-green: #39FF14;
    --pop-blue: #4D4DFF;
    --border-color: #333;
    --font-display: 'Press Start 2P', monospace;
    --font-body: 'Space Grotesk', sans-serif;
}

html {
    scroll-behavior: smooth;
    scrollbar-width: thin;
    scrollbar-color: var(--pop-magenta) var(--bg-primary);
}

body {
    font-family: var(--font-body);
    background-color: var(--bg-primary);
    color: var(--text-primary);
    overflow-x: hidden;
    -webkit-font-smoothing: antialiased;
}

::selection {
    background: var(--pop-yellow);
    color: #000;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: var(--bg-primary); }
::-webkit-scrollbar-thumb { background: var(--pop-magenta); border-radius: 3px; }

a { text-decoration: none; color: inherit; }
img { max-width: 100%; height: auto; display: block; }

/* ==================== NAVIGATION ==================== */
.navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    padding: 1.2rem 0;
    transition: all 0.3s ease;
}

.navbar.scrolled {
    background: rgba(13, 13, 13, 0.92);
    backdrop-filter: blur(20px);
    border-bottom: 2px solid var(--pop-yellow);
    padding: 0.8rem 0;
}

.nav-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.nav-logo {
    font-family: var(--font-display);
    font-size: 0.7rem;
    letter-spacing: 2px;
    color: var(--pop-yellow);
    transition: all 0.3s ease;
    text-shadow: 2px 2px 0 var(--pop-magenta);
}

.nav-links {
    display: flex;
    gap: 2.5rem;
}

.nav-link {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--text-secondary);
    transition: all 0.3s ease;
    position: relative;
    padding: 0.5rem 0;
}

.nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 3px;
    background: var(--pop-cyan);
    transition: width 0.3s ease;
}

.nav-link:hover,
.nav-link.active {
    color: var(--pop-cyan);
}

.nav-link:hover::after,
.nav-link.active::after {
    width: 100%;
}

.nav-socials {
    display: flex;
    gap: 1rem;
}

.social-link {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid var(--border-color);
    border-radius: 4px;
    color: var(--text-secondary);
    transition: all 0.3s ease;
}

.social-link:hover {
    border-color: var(--pop-magenta);
    color: var(--pop-magenta);
    transform: translateY(-2px);
    box-shadow: 0 4px 0 var(--pop-magenta);
}

.nav-menu-btn {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 5px;
}

.nav-menu-btn span {
    width: 24px;
    height: 2px;
    background: var(--pop-yellow);
    transition: all 0.3s ease;
}

.nav-menu-btn.active span:nth-child(1) { transform: rotate(45deg) translate(5px, 5px); }
.nav-menu-btn.active span:nth-child(2) { opacity: 0; }
.nav-menu-btn.active span:nth-child(3) { transform: rotate(-45deg) translate(5px, -5px); }

/* ==================== MOBILE MENU ==================== */
.mobile-menu {
    position: fixed;
    inset: 0;
    background: var(--bg-primary);
    z-index: 999;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 3rem;
    opacity: 0;
    visibility: hidden;
    transition: all 0.4s ease;
}

.mobile-menu.active {
    opacity: 1;
    visibility: visible;
}

.mobile-menu-links {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
}

.mobile-link {
    font-family: var(--font-display);
    font-size: 1.2rem;
    letter-spacing: 3px;
    color: var(--pop-yellow);
    transition: color 0.3s ease;
}

.mobile-link:hover { color: var(--pop-cyan); }

.mobile-menu-socials {
    display: flex;
    gap: 2rem;
}

.mobile-menu-socials a {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 2px;
    color: var(--text-secondary);
    transition: color 0.3s ease;
}

.mobile-menu-socials a:hover { color: var(--pop-magenta); }

/* ==================== HERO SECTION ==================== */
.hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding: 8rem 2rem 4rem;
}

.hero-bg-dots {
    position: absolute;
    inset: 0;
    background-image:
        radial-gradient(circle, rgba(255, 0, 255, 0.08) 1px, transparent 1px);
    background-size: 30px 30px;
    mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
}

.hero-content {
    max-width: 1400px;
    margin: 0 auto;
    width: 100%;
    position: relative;
    z-index: 2;
}

.hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 3px;
    color: var(--pop-green);
    background: rgba(57, 255, 20, 0.1);
    border: 2px solid var(--pop-green);
    padding: 0.5rem 1.2rem;
    border-radius: 0;
    margin-bottom: 2rem;
    animation: fadeInUp 0.8s ease forwards;
    opacity: 0;
}

.badge-dot {
    width: 8px;
    height: 8px;
    background: var(--pop-green);
    animation: pulse 1.5s infinite;
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
}

.hero-title {
    margin-bottom: 1.5rem;
    animation: fadeInUp 0.8s ease 0.15s forwards;
    opacity: 0;
}

.title-line {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 8vw, 7rem);
    line-height: 1.1;
    letter-spacing: -2px;
    color: var(--pop-yellow);
    text-shadow: 4px 4px 0 var(--pop-red), -2px -2px 0 var(--pop-cyan);
}

.title-outline {
    -webkit-text-stroke: 3px var(--pop-magenta);
    color: transparent;
    text-shadow: none;
    position: relative;
}

.title-outline::after {
    content: 'PUPPETS';
    position: absolute;
    left: 4px;
    top: 4px;
    -webkit-text-stroke: 3px var(--pop-cyan);
    opacity: 0.4;
    z-index: -1;
}

.hero-subtitle {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 500px;
    margin-bottom: 2.5rem;
    animation: fadeInUp 0.8s ease 0.3s forwards;
    opacity: 0;
}

.hero-cta {
    display: flex;
    gap: 1rem;
    margin-bottom: 3.5rem;
    animation: fadeInUp 0.8s ease 0.45s forwards;
    opacity: 0;
}

/* Buttons — Pop Art */
.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.9rem 2rem;
    font-family: var(--font-body);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 2px;
    border-radius: 0;
    transition: all 0.2s ease;
    cursor: pointer;
    text-transform: uppercase;
}

.btn-primary {
    background: var(--pop-yellow);
    color: #000;
    border: 3px solid #000;
    box-shadow: 4px 4px 0 #000;
}

.btn-primary:hover {
    transform: translate(-2px, -2px);
    box-shadow: 6px 6px 0 #000;
}

.btn-primary:active {
    transform: translate(2px, 2px);
    box-shadow: 1px 1px 0 #000;
}

.btn-secondary {
    background: transparent;
    color: var(--pop-cyan);
    border: 3px solid var(--pop-cyan);
    box-shadow: 4px 4px 0 var(--pop-cyan);
}

.btn-secondary:hover {
    background: var(--pop-cyan);
    color: #000;
    transform: translate(-2px, -2px);
    box-shadow: 6px 6px 0 rgba(0, 229, 255, 0.4);
}

/* Hero Stats */
.hero-stats {
    display: flex;
    gap: 3rem;
    animation: fadeInUp 0.8s ease 0.6s forwards;
    opacity: 0;
}

.stat {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
}

.stat-number {
    font-family: var(--font-display);
    font-size: 1.5rem;
    color: var(--pop-magenta);
}

.stat-label {
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--text-secondary);
}

/* Hero Images */
.hero-images {
    position: absolute;
    right: 5%;
    top: 50%;
    transform: translateY(-50%);
    z-index: 1;
}

.hero-image-card {
    width: 200px;
    height: 200px;
    overflow: hidden;
    border: 4px solid var(--pop-yellow);
    position: absolute;
    transition: all 0.5s ease;
    box-shadow: 6px 6px 0 var(--pop-magenta);
}

.hero-image-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.hero-image-card:hover {
    z-index: 10;
    transform: scale(1.08) !important;
    border-color: var(--pop-cyan);
    box-shadow: 8px 8px 0 var(--pop-red);
}

.card-1 {
    top: -180px;
    right: 0;
    transform: rotate(-5deg);
    animation: floatCard 5s ease-in-out infinite;
}

.card-2 {
    top: -20px;
    right: 160px;
    transform: rotate(3deg);
    border-color: var(--pop-cyan);
    box-shadow: 6px 6px 0 var(--pop-green);
    animation: floatCard 5s ease-in-out 1s infinite;
}

.card-3 {
    top: 120px;
    right: 20px;
    transform: rotate(-2deg);
    border-color: var(--pop-red);
    box-shadow: 6px 6px 0 var(--pop-yellow);
    animation: floatCard 5s ease-in-out 2s infinite;
}

@keyframes floatCard {
    0%, 100% { transform: translateY(0) rotate(var(--rotate, 0deg)); }
    50% { transform: translateY(-12px) rotate(var(--rotate, 0deg)); }
}

.card-1 { --rotate: -5deg; }
.card-2 { --rotate: 3deg; }
.card-3 { --rotate: -2deg; }

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

/* ==================== GIF SHOWCASE ==================== */
.gif-showcase {
    padding: 3rem 2rem;
    display: flex;
    justify-content: center;
    align-items: center;
    background: var(--bg-secondary);
    position: relative;
    overflow: hidden;
}

.gif-showcase::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
        radial-gradient(circle at 20% 50%, rgba(255, 0, 255, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 80% 50%, rgba(57, 255, 20, 0.08) 0%, transparent 50%);
}

.gif-showcase-inner {
    position: relative;
    width: 100%;
    max-width: 1100px;
    border: 5px solid var(--pop-green);
    box-shadow: 8px 8px 0 var(--pop-magenta), 0 0 40px rgba(57, 255, 20, 0.25);
    overflow: hidden;
    background: var(--bg-card);
}

.gif-showcase-inner img,
.gif-showcase-inner video {
    width: 100%;
    display: block;
    height: auto;
    object-fit: contain;
}



/* ==================== MARQUEE ==================== */
.marquee-section {
    border-top: 3px solid var(--pop-yellow);
    border-bottom: 3px solid var(--pop-yellow);
    padding: 1rem 0;
    overflow: hidden;
    background: var(--pop-red);
}

.marquee {
    overflow: hidden;
    white-space: nowrap;
}

.marquee-content {
    display: inline-flex;
    gap: 2rem;
    animation: marqueeScroll 25s linear infinite;
    font-family: var(--font-display);
    font-size: 0.7rem;
    letter-spacing: 3px;
    color: var(--pop-yellow);
}

.marquee-dot { color: #000; }

@keyframes marqueeScroll {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
}

/* ==================== WAITLIST ==================== */
.waitlist {
    padding: 5rem 2rem;
    text-align: center;
    position: relative;
    background:
        radial-gradient(circle at 50% 50%, rgba(255, 0, 255, 0.06) 0%, transparent 60%),
        var(--bg-primary);
}

.waitlist-container {
    max-width: 600px;
    margin: 0 auto;
}

.waitlist-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 3px;
    color: var(--pop-magenta);
    background: rgba(255, 0, 255, 0.1);
    border: 2px solid var(--pop-magenta);
    padding: 0.4rem 1rem;
    margin-bottom: 1.5rem;
}

.waitlist-title {
    font-family: var(--font-display);
    font-size: clamp(1.4rem, 4vw, 2.2rem);
    letter-spacing: 2px;
    color: var(--pop-yellow);
    text-shadow: 3px 3px 0 var(--pop-magenta);
    margin-bottom: 0.8rem;
}

.waitlist-desc {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 2rem;
}

.waitlist-form {
    display: flex;
    flex-direction: column;
    gap: 0;
    border: 3px solid var(--pop-yellow);
    background: var(--bg-secondary);
    transition: border-color 0.3s, box-shadow 0.3s;
}

.waitlist-form:focus-within {
    border-color: var(--pop-cyan);
    box-shadow: 0 0 20px rgba(0, 229, 255, 0.2);
}

.waitlist-input {
    flex: 1;
    background: transparent;
    border: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    outline: none;
    color: var(--text-primary);
    font-family: var(--font-body);
    font-size: 0.9rem;
    padding: 0.9rem 1.2rem;
    letter-spacing: 0.5px;
}

.waitlist-input:last-of-type {
    border-bottom: none;
}

.waitlist-input::placeholder {
    color: #555;
    letter-spacing: 1px;
}

.waitlist-btn {
    font-family: var(--font-display);
    font-size: 0.5rem;
    letter-spacing: 3px;
    background: var(--pop-yellow);
    color: #000;
    border: none;
    padding: 0.9rem 1.8rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.2s;
    white-space: nowrap;
}

.waitlist-btn:hover {
    background: var(--pop-green);
}

.waitlist-btn svg {
    stroke: #000;
}

.waitlist-status {
    margin-top: 1rem;
    font-family: var(--font-display);
    font-size: 0.4rem;
    letter-spacing: 2px;
    min-height: 1.4rem;
}

.waitlist-status.success {
    color: var(--pop-green);
}

.waitlist-status.error {
    color: var(--pop-red);
}

.waitlist-count {
    margin-top: 0.5rem;
    font-size: 0.75rem;
    color: var(--text-secondary);
    letter-spacing: 1px;
}

/* ==================== FOOTER ==================== */
.footer {
    border-top: 3px solid var(--pop-magenta);
    padding: 3rem 0;
}

.footer-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
}

.footer-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 3rem;
}

.footer-logo {
    font-family: var(--font-display);
    font-size: 0.65rem;
    letter-spacing: 2px;
    color: var(--pop-yellow);
    text-shadow: 2px 2px 0 var(--pop-magenta);
}

.footer-tagline {
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin-top: 0.5rem;
}

.footer-links {
    display: flex;
    gap: 2rem;
}

.footer-social {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--text-secondary);
    transition: all 0.3s ease;
}

.footer-social:hover {
    color: var(--pop-cyan);
}

.footer-bottom {
    display: flex;
    justify-content: space-between;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border-color);
    font-size: 0.75rem;
    color: var(--text-secondary);
    letter-spacing: 1px;
}

/* ==================== SCROLL ANIMATIONS ==================== */
.reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: all 0.8s ease;
}

.reveal.visible {
    opacity: 1;
    transform: translateY(0);
}

/* ==================== RESPONSIVE ==================== */
@media (max-width: 1024px) {
    .hero-images { display: none; }

    .hero {
        flex-direction: column;
        text-align: center;
    }

    .hero-content {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .hero-subtitle {
        text-align: center;
    }

    .hero-cta {
        justify-content: center;
    }

    .hero-stats {
        justify-content: center;
    }
}

@media (max-width: 768px) {
    .nav-links, .nav-socials { display: none; }
    .nav-menu-btn { display: flex; }

    .hero {
        padding: 7rem 1.5rem 3rem;
        min-height: auto;
    }

    .hero-cta {
        flex-direction: column;
        width: 100%;
    }

    .hero-cta .btn {
        width: 100%;
        justify-content: center;
    }

    .hero-stats {
        flex-direction: row;
        gap: 1.5rem;
        flex-wrap: wrap;
        justify-content: center;
    }

    .gif-showcase-inner {
        border-width: 3px;
        box-shadow: 5px 5px 0 var(--pop-magenta), 0 0 25px rgba(57, 255, 20, 0.2);
    }

    .gif-showcase-inner img,
    .gif-showcase-inner video {
        width: 100%;
        height: auto;
    }

    .gif-showcase {
        padding: 3rem 1.5rem;
    }

    .hero-title .title-line {
        font-size: clamp(2rem, 10vw, 4rem);
    }

    .marquee-content {
        font-size: 0.55rem;
    }

    .footer-top {
        flex-direction: column;
        gap: 2rem;
        text-align: center;
    }

    .footer-links {
        justify-content: center;
    }

    .footer-bottom {
        flex-direction: column;
        gap: 0.5rem;
        text-align: center;
    }
}

@media (max-width: 480px) {
    .mobile-link { font-size: 1rem; }

    .hero {
        padding: 6rem 1rem 2rem;
    }

    .hero-badge {
        font-size: 0.5rem;
        padding: 0.4rem 0.8rem;
    }

    .gif-showcase {
        padding: 1.5rem 0.75rem;
    }

    .gif-showcase-inner {
        border-width: 3px;
        box-shadow: 4px 4px 0 var(--pop-magenta), 0 0 20px rgba(57, 255, 20, 0.2);
    }

    .gif-showcase-inner img,
    .gif-showcase-inner video {
        width: 100%;
        height: auto;
    }

    .hero-stats {
        gap: 1rem;
    }

    .stat-number {
        font-size: 1.2rem;
    }

    .btn {
        padding: 0.8rem 1.5rem;
        font-size: 0.7rem;
    }
}
